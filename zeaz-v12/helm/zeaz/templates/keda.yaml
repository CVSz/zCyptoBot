apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: zeaz-api
spec:
  scaleTargetRef:
    name: zeaz-api
  minReplicaCount: 2
  maxReplicaCount: 20
  triggers:
  - type: cpu
    metadata:
      type: Utilization
      value: "70"
