apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: zeaz
spec:
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: zeaz-api
                port:
                  name: http
