apiVersion: apps/v1
kind: Deployment
metadata:
  name: clickhouse
  namespace: zeaz
spec:
  replicas: 1
  selector:
    matchLabels: { app: clickhouse }
  template:
    metadata:
      labels: { app: clickhouse }
    spec:
      containers:
        - name: clickhouse
          image: clickhouse/clickhouse-server
