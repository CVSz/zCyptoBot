name: Provenance

on: [push]

jobs:
  provenance:
    permissions:
      id-token: write
      contents: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: slsa-framework/slsa-github-generator@v1
        with:
          build-type: docker
