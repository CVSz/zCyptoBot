apiVersion: apps/v1
kind: Deployment
metadata:
  name: service_0080
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service_0080
  template:
    metadata:
      labels:
        app: service_0080
    spec:
      containers:
      - name: service_0080
        image: ghcr.io/example/service_0080:latest
        ports:
        - containerPort: 80
