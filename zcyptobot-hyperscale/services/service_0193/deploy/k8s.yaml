apiVersion: apps/v1
kind: Deployment
metadata:
  name: service_0193
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service_0193
  template:
    metadata:
      labels:
        app: service_0193
    spec:
      containers:
      - name: service_0193
        image: ghcr.io/example/service_0193:latest
        ports:
        - containerPort: 80
