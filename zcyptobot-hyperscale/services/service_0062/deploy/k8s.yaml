apiVersion: apps/v1
kind: Deployment
metadata:
  name: service_0062
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service_0062
  template:
    metadata:
      labels:
        app: service_0062
    spec:
      containers:
      - name: service_0062
        image: ghcr.io/example/service_0062:latest
        ports:
        - containerPort: 80
