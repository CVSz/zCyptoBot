apiVersion: apps/v1
kind: Deployment
metadata:
  name: service_0129
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service_0129
  template:
    metadata:
      labels:
        app: service_0129
    spec:
      containers:
      - name: service_0129
        image: ghcr.io/example/service_0129:latest
        ports:
        - containerPort: 80
