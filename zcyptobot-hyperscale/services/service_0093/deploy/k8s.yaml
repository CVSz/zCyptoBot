apiVersion: apps/v1
kind: Deployment
metadata:
  name: service_0093
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service_0093
  template:
    metadata:
      labels:
        app: service_0093
    spec:
      containers:
      - name: service_0093
        image: ghcr.io/example/service_0093:latest
        ports:
        - containerPort: 80
