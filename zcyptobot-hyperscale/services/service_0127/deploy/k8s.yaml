apiVersion: apps/v1
kind: Deployment
metadata:
  name: service_0127
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service_0127
  template:
    metadata:
      labels:
        app: service_0127
    spec:
      containers:
      - name: service_0127
        image: ghcr.io/example/service_0127:latest
        ports:
        - containerPort: 80
