apiVersion: v1
kind: Service
metadata:
  name: zcyptobot
spec:
  type: {{ .Values.service.type }}
  selector:
    app: zcyptobot
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
