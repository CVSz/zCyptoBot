# Infra Strategy

- Multi-cloud federation (active-active)
- Global edge routing (latency + cost + carbon)
- AI control plane (self-optimizing)
- Data moat (usage + pricing intelligence)

Goal:
- Become routing layer above cloud providers
