# OIDC Federation

This deployment supports issuer federation through Dex with an upstream OIDC connector. Configure trust policy so only approved upstream issuer metadata and signing keys are accepted.
