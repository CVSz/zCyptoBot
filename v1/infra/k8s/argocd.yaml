apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd
  namespace: platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: argocd
  template:
    metadata:
      labels:
        app: argocd
    spec:
      containers:
        - name: argocd
          image: argoproj/argocd
