apiVersion: kafka.strimzi.io/v1beta2
kind: Kafka
metadata:
  name: kafka
  namespace: platform
spec:
  kafka:
    replicas: 3
    storage:
      type: persistent-claim
      size: 50Gi
  zookeeper:
    replicas: 3
