groups:
  - name: zeaz
    rules:
      - alert: PodDown
        expr: up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Pod is down"
          description: "A monitored target has been down for more than 1 minute."
