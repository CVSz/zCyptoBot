apiVersion: v1
kind: LimitRange
metadata:
  name: workload-default-limits
  namespace: trading
spec:
  limits:
    - type: Container
      default:
        cpu: "500m"
        memory: "512Mi"
      defaultRequest:
        cpu: "100m"
        memory: "128Mi"
